package client.view;


public class TestGuiRotateImage {

	// @Test
	// public void rotateAnImage() {
	// GuiLoadImageResource guiLoadImageResource = new GuiLoadImageResource();
	// ImageIcon[] sizedIcons = guiLoadImageResource.getSizedIcons(
	// GuiConstants.SHEEPHERD[0], GuiConstants.DIM_LABEL_MOVE * 2 / 3,
	// GuiConstants.DIM_LABEL_MOVE);
	// GuiRotateImage guiRotateImage = new GuiRotateImage();
	// Image rotateImage = guiRotateImage.rotateImage(
	// sizedIcons[0].getImage(), 90);
	// assertTrue(rotateImage != null);
	// }
}
